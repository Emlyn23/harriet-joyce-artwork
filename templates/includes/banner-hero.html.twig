        <div class="hero-body bottom-border" style="background-image: url({{ background_img }}); background-size: cover; background-position: center;">
            {% include "includes/navbar.html.twig" with {
                'visibility': 'bg-transparent'
            } %}
            <div class="container">
                <h1>
                {{ title }}
                </h1>
                <h4>
                {{ subtitle }}
                </h4>
            </div>
        </div>